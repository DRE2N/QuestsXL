package de.erethon.questsxl.tool;

import de.erethon.questsxl.common.ObjectiveHolder;
import org.bukkit.util.StringUtil;

public class QMessage {

    public static String parse(String msg, ObjectiveHolder objectiveHolder) {
        // Todo: Parse placeholders like global score / event score / player score / etc.
        return msg;
    }
}
